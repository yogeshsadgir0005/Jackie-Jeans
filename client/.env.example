# Copy to .env for local builds, or set in your host (Vercel/Netlify) dashboard.

# Origin of the deployed Express API. Leave empty in local dev — Vite proxies
# /api to http://localhost:5000 automatically.
# Example: VITE_API_BASE=https://jackie-jeans-api.onrender.com
VITE_API_BASE=
